# Server environment example (do not commit real secrets)

# Runtime
NODE_ENV=production
PORT=4000

# Secrets (generate securely and keep private)
JWT_SECRET=replace-with-hex-64
ENCRYPTION_KEY=replace-with-32-characters-min

# External services
TT_BASE_URL=https://talkytimes.com

# Database
# For PostgreSQL (recommended for production):
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public

# For SQLite (dev only):
# DATABASE_URL=file:./prisma/dev.db
